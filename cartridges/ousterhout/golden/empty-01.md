<!--
golden input · state: empty · by-construction label
The user triggered the guru but attached NO artifact to review (no PRD, no design doc, no described
decision) — only the bare invocation below. There is nothing to critique.
Designed skill behavior (E-ref, deterministic): ASK for the artifact; do NOT fabricate a Complexity
Diagnosis / Red Flags / Design-it-Twice critique of an empty input. A correct response declines the
full output contract and requests the missing design content, so it emits FEWER than all 5 sections.
This is the "empty" row of the state matrix, distinct from "off-domain" (a real but wrong-topic doc).
-->

/ousterhout-guru
